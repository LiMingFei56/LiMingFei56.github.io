---
layout: post
title: numpy 
categories: lib
tags: [lib]
date: 2026-03-14
---


## numpy

### create array

np.sctypes

x = np.array([1, 2, 3, 4])

print(type(x))
print(x.ndim)
print(x.shape)

* 元素类型
x.dtype
y = np.array(data, dtype=np.int)
y.astype(np.complex)

np.zeros((2, 3))
np.ones((2, 3))
np.full((2, 3), -1)
np.empty((2, 3))
np.zeros_like(x)
np.full_like(x)

* 单位矩阵
np.identity(3)

* 对角矩阵
np.diag([1, 2, 4], k=-1)
np.diagflat([1, 2, 4], k=-1)

* 三角矩阵
np.tri(3)

* 等间距变化
np.arange(1, 6, 2)
np.linspace(0, 1, 5)
np.logspace(0, 3, 4)

### 数组访问

x[:4]
x[1, 2]
x[:, 2]
x[:3].copy()

x[[1, 3]]
print(x > 4)
print(x[x > 4])

### 数据形状和大小变更

x.reshape(4)
np.expand_dims(y, axis=1)
y[:, np.newaxis]

np.vstack((x, x, x))
np.hstack((x, x, x))
np.stack((x, x, x), axis=1)

* 重置合并

A = np.eye(2)
B = np.zeros((2, 3))
C = np.ones((3, 2))
D = np.eye(3) * 2

np.block([[A, B], [C, D]])


np.tile(x, (2, 1))

np.repeat(x, 3, axis=1)

* 转置

np.transpose(x)
x.T
x.swapaxes(0, 1)

### 运算

不同形状的数据运算 需要广播成一样大小的

x + y
x - y
x * y Hadamard product
x / y

* ufunc
- sin
- deg2rad
- exp
- log
- sqrt
- abs
- rint

* 比较运算

* 矩阵乘积(Matrix product)

A @ B
np.dot



## Reference
